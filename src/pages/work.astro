---
import CustomLink from "@components/CustomLink.astro";
import MainLayout from "@layouts/MainLayout.astro";
import Contact from "@sections/Contact.astro";
const placeholderProjects = [
  {
    title: "Client Website #1",
    description: "Highlight your role, the stack, and the measurable impact.",
    link: "#",
  },
  {
    title: "Client Website #2",
    description: "Swap this copy once your second showcase is ready.",
    link: "#",
  },
];
---

<style>
  .heroSection {
    padding-inline: 12px;
  }
  .hero {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .hero p {
    /* max-width: 560px; */
    color: var(--color-text-muted);
  }

  .workSection {
    padding-inline: 12px;
  }

  .projectGrid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
  }

  .projectCard {
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 16px;
    min-height: 280px;
  }

  .projectThumb {
    aspect-ratio: 16 / 9;
    border-radius: 12px;
    background: var(--color-muted);
    display: flex;
    align-items: center;
    justify-content: center;
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
  }

  .projectMeta {
    margin-top: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
  }

  @media screen and (width >= 768px) {
    .heroSection {
      padding-inline: 32px;
    }
    .hero {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-end;
    }

    .workSection {
      padding-inline: 32px;
    }

    .projectGrid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media screen and (width >= 1200px) {
    .heroSection {
      padding-inline: 48px;
    }

    .workSection {
      padding-inline: 48px;
    }
  }
</style>

<MainLayout title="Work" description="Case studies and websites.">
  <section class="heroSection">
    <div class="hero">
      <div>
        <h1>Work</h1>
        <p>
          Showcase websites, ecommerce builds, and digital experiences. Case
          studies and live links are in progress and will be published here
          soon.
        </p>
      </div>
    </div>
  </section>

  <section class="workSection">
    <div class="projectGrid">
      {
        placeholderProjects.map((project) => (
          <article class="projectCard">
            <div class="projectThumb">Placeholder</div>
            <div>
              <h3>{project.title}</h3>
              <p>{project.description}</p>
            </div>
            <div class="projectMeta">
              <CustomLink href={project.link} text="Add link" />
            </div>
          </article>
        ))
      }
    </div>
  </section>

  <Contact />
</MainLayout>
